Required: 

    • Bad functions implementation, should use arrays and indexing instead of If statements.

    • No need for Starting_State as it already initialized as OFF,
        If needed to be on, just use on function.
    Unnecessary feature, may even cause confusions.
     If developer insists on addding this feature, 
        Then move On/Off macros to DC_private.h, 
        OR use Predefined macros like Enabled/Disabled.
    
Recommended Improvements:

    • Unclear Naming of files, DC_Motor_.... is clearer.
